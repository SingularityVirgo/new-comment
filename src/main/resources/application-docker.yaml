# 在 Docker Compose 网络中使用服务名，与 application.yaml 中账号密码、库名、端口保持一致
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/comment?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: "123456"
  data:
    redis:
      host: redis
      port: 6379
      password: "123456"

server:
  port: 8081
